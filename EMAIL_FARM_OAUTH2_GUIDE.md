# 📧 Email Farm - OAuth2 Support Guide

## ✅ Что реализовано

### Backend
- ✅ **OAuth2 IMAP клиент** (`email_oauth2_client.py`)
  - Получение access_token через refresh_token
  - IMAP XOAUTH2 аутентификация
  - Поддержка SOCKS5 прокси
- ✅ **Расширенная база данных**
  - Новые поля: `refresh_token_encrypted`, `client_id`, `auth_type`
  - Автоматическая миграция существующих таблиц
- ✅ **API endpoints**
  - `/api/admin/emails/bulk_upload` - поддержка OAuth2 и password
  - `/api/emails/{id}/check_code` - автовыбор IMAP клиента
- ✅ **Google Sheets логирование**
  - Таблица `PostBD`, лист `Post`
  - Логирование всех операций (allocation, check, ban)

### Frontend
- ✅ **Переключатель OAuth2 / Password**
- ✅ **Модальное окно настройки прокси**
- ✅ **Автоматическое присвоение прокси**
- ✅ **Динамическая смена формата и placeholder**

---

## 🎯 Формат загрузки почт

### OAuth2 (Outlook с токенами)
```
email:password:refresh_token:client_id
```

**Пример:**
```
test1@outlook.com:Pass123!:M.C510_BAY.0.U.-Cte8bWZ...:9e5f94bc-e8a4-4e73-b8be-63364c29d753
test2@outlook.com:Pass456!:M.C533_SN1.0.U.-CjYPRH7G...:9e5f94bc-e8a4-4e73-b8be-63364c29d753
```

### Password (обычная аутентификация)
```
email:password:proxy
```

или без прокси:
```
email:password
```

**Пример:**
```
test1@outlook.com:Pass123!:socks5://user:pass@192.168.1.1:1080
test2@outlook.com:Pass456!
```

---

## 🔧 Как настроить прокси

### Вариант 1: Вставить в строку с почтой (Password режим)
```
test@outlook.com:Password123:socks5://user:pass@ip:port
```

### Вариант 2: Использовать модальное окно "🔧 Прокси" (для OAuth2)

1. Нажми кнопку **"🔧 Прокси"** в админ-панели Email Farm
2. Вставь список SOCKS5 прокси (по одному на строку):
   ```
   socks5://user1:pass1@192.168.1.1:1080
   socks5://user2:pass2@192.168.1.2:1080
   socks5://user3:pass3@192.168.1.3:1080
   ```
3. Нажми **"💾 Сохранить прокси"**
4. Теперь при загрузке OAuth2 почт прокси будут автоматически присваиваться по кругу

**Как это работает:**
- Прокси сохраняются в `localStorage` браузера
- При загрузке почт система берет прокси из списка циклически
- Если у тебя 3 прокси и 10 почт → прокси распределятся: 1→2→3→1→2→3→1→2→3→1

---

## 📋 Пошаговая инструкция

### Шаг 1: Подготовь почты

Твои почты в формате:
```
email:password:M.C***_TOKEN:client_id
```

Это правильный формат для OAuth2! ✅

### Шаг 2: (Опционально) Настрой прокси

Если хочешь использовать прокси:
1. Нажми **"🔧 Прокси"**
2. Вставь список SOCKS5 прокси
3. Сохрани

Прокси будут автоматически присвоены каждой почте.

### Шаг 3: Загрузи почты

1. Открой админ-панель → **"📧 Email Farm"**
2. Выбери переключатель **"OAuth2 (Outlook)"**
3. Вставь почты в textarea:
   ```
   test1@outlook.com:Pass123!:M.C510_BAY...:9e5f94bc-...
   test2@outlook.com:Pass456!:M.C533_SN1...:9e5f94bc-...
   ```
4. Нажми **"📤 Загрузить почты"**

### Шаг 4: Проверь результат

- ✅ Статистика обновится (Всего почт / Свободных)
- ✅ Почты появятся в SQLite базе (`tiktok_analytics.db`)
- ✅ Логи появятся в Google Sheets (`PostBD` → лист `Post`)

---

## 🧪 Как работает OAuth2

### Процесс аутентификации:

1. **Получение access_token**
   ```python
   POST https://login.live.com/oauth20_token.srf
   Body: {
       client_id: "твой_client_id",
       grant_type: "refresh_token",
       refresh_token: "твой_refresh_token"
   }
   ```

2. **IMAP подключение**
   ```python
   # Генерация auth string
   auth_string = f"user={email}\1auth=Bearer {access_token}\1\1"

   # IMAP аутентификация
   imap.authenticate('XOAUTH2', auth_string)
   ```

3. **Получение писем**
   - Выбирает INBOX
   - Получает последние 5 писем
   - Извлекает subject, body, from, date

4. **Умная фильтрация**
   - Проверяет на фишинг (30+ фраз на EN/RU/UA)
   - Извлекает verification код (6 цифр)
   - Отправляет алерты админам при опасных письмах

---

## 📊 Google Sheets (PostBD)

### Структура листа "Post"

| Email | Status | User ID | Username | Allocated At | Last Checked | Ban Reason | Total Checks | Has Proxy | Notes |
|-------|--------|---------|----------|--------------|--------------|------------|--------------|-----------|-------|
| test@outlook.com | active | 123456 | @username | 2025-12-10 15:30 | 2025-12-10 16:45 | | 3 | Да | ✅ Код найден: Your verification code |

### Что логируется:

**При выделении почты (`allocate`):**
- Email, User ID, Username
- Status → `active`
- Allocated At → текущее время
- Has Proxy → `Да` / `Нет`

**При проверке кода (`check_code`):**
- Last Checked → текущее время
- Total Checks → +1
- Notes:
  - ✅ "Код найден: тема письма"
  - ⚠️ "ПОДОЗРИТЕЛЬНО: тема + причина"
  - 📭 "Нет писем"

**При бане (`mark_banned`):**
- Status → `banned`
- Ban Reason → "User reported as banned/invalid"
- Notes → 🚫 "Забанена: причина"

---

## ⚠️ Важные моменты

### 1. Refresh Token имеет срок действия
- OAuth2 refresh_token может истечь (обычно через 90 дней)
- Если почта не подключается → refresh_token устарел
- Нужно будет получить новый токен у продавца

### 2. Client ID должен совпадать
- Все твои почты используют один `client_id`
- Это нормально для массовых аккаунтов

### 3. Прокси опциональны
- OAuth2 почты работают и без прокси
- Но Outlook может заблокировать при частых подключениях с одного IP
- Рекомендуем использовать прокси для каждой почты

### 4. IMAP должен быть включен
- Продавец пишет "POP3, SMTP, IMAP включены" ✅
- Это значит можно подключаться через IMAP

---

## 🚀 Что дальше?

### Проверь работу локально (опционально)

Запусти backend локально:
```bash
cd webapp/backend
uvicorn main:app --reload
```

Протестируй через Postman:
```bash
POST http://localhost:8000/api/admin/emails/bulk_upload
Headers:
  x-telegram-init-data: твой_init_data
  Content-Type: application/json
Body:
{
  "accounts": [
    {
      "email": "test@outlook.com",
      "password": "Pass123",
      "refresh_token": "M.C510_BAY...",
      "client_id": "9e5f94bc-...",
      "auth_type": "oauth2",
      "proxy": "socks5://user:pass@ip:port"
    }
  ]
}
```

### Проверь на Render

После деплоя проверь логи:
```
✅ Email Farm system initialized
✅ Email Sheets Manager (PostBD) initialized
```

Если видишь эти сообщения → всё работает! 🎉

---

## 📞 Возможные проблемы

### ❌ "Failed to connect to email"
**Причины:**
- Refresh token истек
- Client ID неверный
- Прокси не работает
- IMAP не включен у почты

**Решение:**
- Проверь, что refresh_token и client_id правильные
- Попробуй без прокси
- Проверь срок действия токена у продавца

### ❌ "OAuth2 credentials missing"
**Причина:** Не указаны refresh_token или client_id

**Решение:** Загрузи почты в правильном формате:
```
email:password:refresh_token:client_id
```

### ❌ Почта загружается, но не проверяется
**Причина:** Не хватает прав или IMAP отключен

**Решение:** Убедись что в описании продавца написано "IMAP включен"

---

✅ **Всё готово к использованию!**

Твой Email Farm теперь поддерживает:
- ✅ OAuth2 аутентификацию (Outlook)
- ✅ Обычную password аутентификацию
- ✅ SOCKS5 прокси (опционально)
- ✅ Автоматическую ротацию прокси
- ✅ Google Sheets логирование (PostBD)
- ✅ Умную фильтрацию опасных писем

🎉 Загружай почты и тестируй!
